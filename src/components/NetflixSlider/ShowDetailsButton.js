import React from "react";
import "./ShowDetailsButton.scss";

const ShowDetailsButton = ({ title }) => (
  <div className="show-details-button">
    <span>{title}</span>
  </div>
);

export default ShowDetailsButton;
